version: 2.1

orbs:
  node: circleci/node@5.0.2
  heroku: circleci/heroku@1.2.6

jobs:
  build:
    executor: node/default

    steps: 
      - checkout
      - node/install-packages:
        pkg-manager: npm
      - run:
        comman: npm start
        name: start server

      

